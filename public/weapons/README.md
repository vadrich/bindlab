# Weapon card icons (CS2 buy menu)

Official CS2 equipment SVGs (from game files) converted to **white PNG silhouettes**.

Regenerate:

```bash
npm run icons
```

Files are named by item `id` from `src/data/items.ts`, e.g. `mp7.png` → `/weapons/mp7.png`.
